#include <stdio.h>
#include <type.h>

/**
 * main - Prints the alphabets
 * Return: Always 0
 */
int main(void)
{
int lowercase = 'a';
while (lowecase < = 'z')
putchar (lowercase);
lowercase + = 1;
putchar('\n');
return (0);
}
