#include <stdio.h>

/**
*main - Empty point
*
*Return: Always 0
*/

int main(void)
{
lastnum = n % 10;
if (lastnum > 5)
printf("Last digit is %d and %d is greater than 5\n", n, lastnum);
else if (lastsum == 0)
printf("Last digit of %d is %d and is 0\n", n, lastsum);
else if (lastsum < 6 && lastsum != 0)
printf("Last didgit of %d is %d and is less than 6 and not 0\n", n, lastsum);
return (0);
}


